// Obtener referencia de un elemento html por el ID



const form = document.getElementById("formulario")

// Agregar evento al elemento seleccionado

const onSubmit = (ev)=>{
    ev.preventDefault
    console.log(DragEvent)
}

form.addEventListener("submit", onSubmit)